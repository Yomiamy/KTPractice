package com.imformation_checker.utils

object ConstantUtils {

    object AppInfo {
        const val DB_NAME = "ktPractice.db"
    }

    object Network {
        const val DEFAULT_TIMEOUT = 30L
    }

    object Count {
        const val PERSON_LIST_PAGE_SIZE = 4
    }

    object Api {
        const val SERVER_DOMAIN = "https://us-central1-redso-challenge.cloudfunctions.net"
    }
}